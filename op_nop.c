#include "monty.h"

/**
 * op_nop - Perform no operation.
 * @top: Pointer to the top of the stack.
 * @line_num: Line number associated with the opcode.
 **/
void op_nop(stack_t **top, unsigned int line_num)
{
	(void)top;
	(void)line_num;
}
